[install]
peer = false

[test]
# Preload shared test infrastructure - ensures jeju CLI is running
preload = ["@jejunetwork/tests/bun-global-setup"]
coverage = true
timeout = 60000
# Only run unit tests (api.test.ts), not Playwright e2e tests
root = "tests/api.test.ts"
